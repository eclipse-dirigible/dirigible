/*
 * Generated by Eclipse Dirigible based on model and template.
 *
 * Contributes this entity's PARTNER page to the Partner shell (the partner counterpart of the shared
 * application shell), via the application-partner-perspectives extension point. The path opens
 * this project's own Harmonia SPA at the partner route in embedded mode.
 *
 * Do not modify the content as it may be re-generated again.
 */
const perspectiveData = {
	id: '${projectName}-partner-${name}',
	label: '${menuLabel}',
	tkey: '${projectName}:${tprefix}.t.${dataName}_plural',
	path: '/services/web/${projectName}/gen/${genFolderName}/index.html?embedded=true#/partner/${name}',
	kind: 'PARTNER',
	// No navigation group is declared here: the Partner shell has a single one, registered as the
	// DEFAULT of its extension point, so the shell owns the placement. Repeating that group id in
	// every generated artifact would be a platform constant they must agree with forever - and a
	// rename of it could then only be repaired by regenerating every module.
#if($perspectiveOrder)
	order: ${perspectiveOrder},
#end
	// Lucide / Harmonia built-in icon NAME (rendered by the shell via x-h-lucide), not a unicons SVG
	// URL - the Harmonia shells resolve a bare name through Lucide; a '<name>.svg' path would 404.
	icon: '${iconName}'
};
if (typeof exports !== 'undefined') {
	exports.getPerspective = () => perspectiveData;
}
