/*
 * Generated by Eclipse Dirigible based on model and template.
 *
 * Contributes this module's ADMINISTRATION page to the Administration shell via the
 * application-admin-perspectives extension point (ADMINISTRATOR-gated at the shell URI).
 * One perspective per MODULE - the page itself lists every entity.
 *
 * Do not modify the content as it may be re-generated again.
 */
const perspectiveData = {
	id: '${projectName}-admin',
	label: '${appTitle}',
	path: '/services/web/${projectName}/gen/${genFolderName}/admin/index.html',
	kind: 'ADMIN',
	// No navigation group is declared here: the Administration shell has a single one, registered as
	// the DEFAULT of its extension point, so the shell owns the placement. Repeating that group id in
	// every generated artifact would be a platform constant they must agree with forever - and a
	// rename of it could then only be repaired by regenerating every module.
	icon: 'wrench'
};
if (typeof exports !== 'undefined') {
	exports.getPerspective = () => perspectiveData;
}
