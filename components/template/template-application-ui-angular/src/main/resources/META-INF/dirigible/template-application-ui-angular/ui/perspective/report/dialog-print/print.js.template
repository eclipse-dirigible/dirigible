const viewData = {
    id: '{{projectName}}-Reports-{{name}}-print',
    label: 'Print',
    path: '/services/web/{{projectName}}/gen/{{genFolderName}}/ui/${perspectiveName}/{{name}}/dialog-print/index.html',
    perspective: '${perspectiveName}',
    view: '{{name}}',
    type: 'page',
    order: 10
};
if (typeof exports !== 'undefined') {
    exports.getView = () => viewData;
}