[
#foreach($role in $roles)
    #if($role.roleRead)
    {
        "name": "${role.roleRead}",
        "description": "A role that grants read only permission for ${role.entityName}."
    }#end#if($role.roleRead && $role.roleRead),#end
    #if($role.roleWrite)
    {
        "name": "${role.roleWrite}",
        "description": "A role that grants full access for ${role.entityName}."
    }
    #end
#end
]